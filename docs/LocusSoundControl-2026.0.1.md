## 2026.0.1

The first beta build. It does not control sound output yet. This release proves signing, notarization and the update chain.

### Added
- A menu bar icon, with a Sound Devices window and Quit. The window is empty for now.
- Check for Updates…, plus scheduled checks, through Sparkle.
- On first launch from outside your Applications folder, an offer to move there. macOS runs apps opened from Downloads out of a read-only temporary copy, where updates cannot install.
